<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}Devoured!{{else}}Eat This!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{id}}">I don't like this burger!</button>
</li>
